# DINEFLOW Web — Restaurant Management Frontend

DINEFLOW Web is an artisanal restaurant web platform built with **React 18** and **styled-components**. It consumes a RESTful **Django 5** backend connected to **Neon PostgreSQL** and deployed serverless on **Vercel**.
